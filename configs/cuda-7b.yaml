# Production profile: Janus-Pro-7B on a CUDA box.
model_id: deepseek-ai/Janus-Pro-7B
device: cuda
dtype: bfloat16
vq_dtype: float32
attn_implementation: auto   # uses flash-attn when installed, warns and uses sdpa otherwise
seed: 42

generation:
  cfg_weight: 5.0
  temperature: 1.0
  parallel_size: 4          # image i still reproduces seed+i exactly, whatever the batch size
